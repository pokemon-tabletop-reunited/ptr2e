<section class="tab sheet-body {{tabs.overview.cssClass}}" data-tab="overview" data-group="sheet">
    <div class="scroll">
        <fieldset>
            <legend>
                Consumable Overview
            </legend>
                {{{enrichedDescription}}}
        </fieldset>
    </div>
</section>