<section class="tab sheet-body {{tabs.overview.cssClass}}" data-tab="overview" data-group="sheet">
    <div class="scroll">
        <fieldset>
            <legend>
                Equipment Overview
            </legend>
                {{{enrichedDescription}}}
        </fieldset>
    </div>
</section>